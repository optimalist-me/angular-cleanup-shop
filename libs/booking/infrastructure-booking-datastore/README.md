# infrastructure-booking-datastore

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build infrastructure-booking-datastore` to build the library.

## Running unit tests

Run `nx test infrastructure-booking-datastore` to execute the unit tests via [Jest](https://jestjs.io).

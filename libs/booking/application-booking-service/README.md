# application-booking-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build application-booking-service` to build the library.

## Running unit tests

Run `nx test application-booking-service` to execute the unit tests via [Jest](https://jestjs.io).

# presentation-booking-rest

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build presentation-booking-rest` to build the library.

## Running unit tests

Run `nx test presentation-booking-rest` to execute the unit tests via [Jest](https://jestjs.io).
